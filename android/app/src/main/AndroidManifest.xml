<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links for invite links only. A scanned group QR code encodes
                 https://app.mybarbets.com/join/<code>?src=qr, and with autoVerify the OS opens
                 this app for it directly instead of the browser, once Google has verified
                 public/.well-known/assetlinks.json on that host against the signing
                 certificate (the Play App Signing key's SHA-256, not the upload key). The
                 web layer then routes it (components/pwa/NativeDeepLink.tsx).

                 pathPrefix is deliberately /join and nothing wider: the same host also serves
                 the emailed password-reset and confirm-signup links, which have to open in a
                 browser (a one-time-token link has no "hand off to the app" equivalent, see
                 lib/mobileGate.ts), so claiming the whole host would break those. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.mybarbets.com" android:pathPrefix="/join" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Without this, FCM falls back to the full app launcher icon for the status-bar
             notification icon - a fully-opaque square that Android's forced monochrome masking
             (API 21+) turns into a plain solid circle instead of a recognizable shape. This is the
             same alpha-only silhouette asset as public/badge-mono.png (same purpose, PWA web push
             badge), generated per-density into drawable-*dpi/ic_stat_notify.png. -->
        <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/ic_stat_notify" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_icon_color" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
